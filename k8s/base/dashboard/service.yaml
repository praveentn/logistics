apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: logistics-demo
  labels:
    app: dashboard
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: dashboard
